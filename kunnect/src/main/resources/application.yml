spring:
  application:
    name: kunnect
  datasource:
    url: jdbc:mariadb://localhost:3306/while1
    username:
    password:
    driver-class-name: org.mariadb.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MariaDBDialect

